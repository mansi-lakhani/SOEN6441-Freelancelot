package models;
import models.*;

/**
 * ProfilResultData test classes to test ProfileResultData class
 * 
 * @author Apekshaba Gohil
 */

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNull;

import org.junit.*;



public class ProfileResultDataTest {

    @Test
    public void getProfiledataTest(){
		ProfileResultData result = new ProfileResultData();
//        result.setProfiledata(new ProfileData());
//
//        assertEquals(0, String.valueOf(result.getProfiledata()));
        
	}

    
}